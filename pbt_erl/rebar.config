%% プラグイン自体
{project_plugins, [rebar3_proper]}.
%% PropEr への依存関係が必要なのは、テストケースをコンパイルするため
%% あくまでもテストの依存関係としてのみ必要
{profiles,
  [{test, [
    {erl_opts, [nowarn_export_all]},
    {deps, [proper]}
  ]}
]}.
